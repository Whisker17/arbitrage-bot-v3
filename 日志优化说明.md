# 日志优化说明

## 已完成的优化

### 1. 移除负利润路径的日志

**之前**：每个负利润的路径都会输出两条日志
```log
INFO v3.sim.detail: Simulated path ... profit=-640455235047
INFO v3.sim.filter: Filtered: non-positive profit ... profit=-640455235047
```

**现在**：负利润路径直接跳过，不输出任何日志

### 2. 减少低利润路径的日志噪音

**之前**：所有低于阈值的路径都会输出日志

**现在**：只记录接近阈值的路径（利润 > 阈值的 50%）

例如，如果阈值是 0.01 MNT：
- 利润 0.006 MNT → 会记录（接近阈值）
- 利润 0.0001 MNT → 不记录（太小，噪音）

### 3. 添加统计摘要

在每个有活动的区块，输出一条汇总日志：

```log
INFO v3.candidate: Path filtering summary 
  block=86448664 
  total_paths=450 
  negative_profit=380 
  below_gross_threshold=45 
  gas_makes_negative=15 
  below_net_threshold=8 
  safety_factor_fail=2 
  passed=0
```

这样你可以一眼看出：
- 总共检查了多少条路径
- 每个过滤条件过滤了多少条路径
- 最终有多少条路径通过

## 新的日志输出示例

### 正常区块（无候选路径）

```log
[INFO v3.block] Processing block block=86448664
[INFO v3.pool] Updated Agni pools block=86448664 changed=3
[INFO v3.candidate] Path filtering summary block=86448664 total_paths=450 negative_profit=380 below_gross_threshold=45 gas_makes_negative=15 below_net_threshold=8 safety_factor_fail=2 passed=0
```

只有 3 条日志，非常简洁！

### 有候选路径的区块

```log
[INFO v3.block] Processing block block=86448665
[INFO v3.pool] Updated Agni pools block=86448665 changed=2
[INFO v3.sim.detail] Simulated path with positive profit block=86448665 path="..." profit=50000000000000000 input=... output=...
[INFO v3.sim.detail] Profit after gas calculation block=86448665 path="..." gross_profit=50000000000000000 net_profit=30000000000000000 hops=3
[INFO v3.candidate] Path filtering summary block=86448665 total_paths=450 negative_profit=375 below_gross_threshold=42 gas_makes_negative=20 below_net_threshold=10 safety_factor_fail=2 passed=1
[INFO v3.candidate] Found profitable candidates block=86448665 count=1
[INFO v3.exec] Selected non-conflicting candidates block=86448665 total_candidates=1
[INFO v3.exec] Sending executeArbitrage signature="..." hops=3 input=...
```

### 接近阈值的路径（会记录）

```log
[INFO v3.sim.detail] Simulated path with positive profit block=86448666 path="..." profit=7000000000000000
[INFO v3.sim.filter] Filtered: below min_gross_profit (close to threshold) block=86448666 profit=7000000000000000 threshold=10000000000000000
```

这条路径的利润是 0.007 MNT，阈值是 0.01 MNT，因为接近阈值所以会记录。

## 日志级别配置

### 推荐配置（生产环境）

```bash
# .env
RUST_LOG=info,v3=info
```

这样你会看到：
- ✅ 区块处理
- ✅ 池子更新
- ✅ 过滤统计摘要
- ✅ 候选路径发现
- ✅ 执行尝试
- ❌ 不会看到每个路径的详细模拟（除非通过所有过滤）

### 调试配置

```bash
# .env
RUST_LOG=info,v3=info,v3.sim.detail=info
```

这样你会额外看到：
- ✅ 每个有正利润的路径的模拟详情
- ✅ Gas 计算后的利润

### 完整调试配置

```bash
# .env
RUST_LOG=debug
```

这样你会看到所有日志，包括内部调试信息。

## 统计摘要字段说明

| 字段 | 说明 |
|------|------|
| `total_paths` | 总共检查的路径数量 |
| `negative_profit` | 负利润的路径数量（最常见） |
| `below_gross_threshold` | 毛利润低于阈值的数量 |
| `gas_makes_negative` | Gas 成本导致净利润为负的数量 |
| `below_net_threshold` | 净利润低于阈值的数量 |
| `safety_factor_fail` | 1.2倍安全系数检查失败的数量 |
| `passed` | 通过所有过滤的路径数量 |

## 从日志中分析问题

### 场景 1：所有路径都是负利润

```log
Path filtering summary ... negative_profit=450 ... passed=0
```

**问题**：市场上没有套利机会，或者池子状态同步有问题

### 场景 2：有正利润但低于阈值

```log
Path filtering summary ... negative_profit=380 below_gross_threshold=45 ... passed=0
```

**问题**：利润阈值设置太高

**解决**：降低 `MIN_GROSS_PROFIT_WEI`

### 场景 3：Gas 成本过高

```log
Path filtering summary ... gas_makes_negative=50 ... passed=0
```

**问题**：Gas 成本估算过高，或者真实 Gas 成本就是很高

**解决**：
1. 检查 `src/arbitrage/gas.rs` 中的 Gas 配置
2. 考虑只执行高利润的机会

### 场景 4：安全系数太严格

```log
Path filtering summary ... safety_factor_fail=20 ... passed=0
```

**问题**：1.2倍安全系数太严格

**解决**：修改代码中的 `1.2` 为 `1.1` 或 `1.0`

## 总结

优化后的日志：
- ✅ **大幅减少噪音**：不再显示负利润路径
- ✅ **提供统计摘要**：一眼看出过滤情况
- ✅ **保留关键信息**：接近阈值的路径仍会记录
- ✅ **易于分析**：通过统计数据快速定位问题

现在你可以重新运行程序，日志会清晰很多！

