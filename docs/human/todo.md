# 最小化重启服务的修复内容

1. 修改对应的 fee model
  - 由于 Mantle 在 Arsia 升级之后改变了 fee model，所以我们需要重新对目前硬编码使用的一些 gas 参数进行修复，包括 gas limit，gas 估计等，我建议进行一次数据分析，去获取目前 Mantle 链上的真实情况，然后我再来进行修改

2. 增加协议数量并通用化 poolLists 获取方式（使用脚本来更新）
  - 目前 Mantle 的 dex 协议数量发生了变化，所以我希望我们在 data 中要先去维护有哪些 dex 协议，然后在去维护这些 dex 协议的关键合约（比如创建池子的合约，这样我们才能爬取到所有的池子），再然后我们去获得所有的池子，然后我们再进行一次池子价值的筛选（低于 1000u 不考虑进来），最终我们在筛选掉那些无法组成套利环路的池子（4 hops 之内的）
  - 我希望上面的操作都可以使用脚本来一键完成，最终的效果应该就是类似这样的：data/poolLists.csv

3. 修复合约 bug
  - 合约存在 bug，需要修复
